package com.example.donutdash.data

import com.example.donutdash.model.Donut

class DonutDatasource {
    fun loadDonuts(): List<Donut> {
        val list = mutableListOf<Donut>()
        for (/* donut in list from flavor fragment */) {
            TODO("add to list and return donut list for ToppingsFragment")
        }
    }
}