package com.example.donutdash.data

import com.example.donutdash.model.Order

class OrderDatasource {
    fun loadOrder(): Order {
        TODO(
            "take order text from end of order Intent, turn it into an order, and call from viewModel " +
                    "to make available when loading LandingFragment"
        )
    }
}